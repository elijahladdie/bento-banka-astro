---
import { Sun, Moon, Monitor } from "@lucide/astro";

export interface Props {
  theme?: "system" | "dark" | "light";
  label: string;
  class?: string;
}

const { theme = "system", label } = Astro.props;
---

<!-- <button
  type="button"
  aria-label={label}
  class="relative inline-flex h-11 w-11 items-center justify-center rounded-2xl border border-white/10 bg-white/[0.03] text-white/80 backdrop-blur-xl transition-all duration-300 hover:border-white/20 hover:bg-white/[0.05]"
  data-theme-toggle
  data-theme-state={theme}
>
  <span data-theme-icon="system" class={theme === "system" ? "" : "hidden"}>
    <Monitor class="h-4 w-4" />
  </span>
  <span data-theme-icon="light" class={theme === "light" ? "" : "hidden"}>
    <Sun class="h-4 w-4" />
  </span>
  <span data-theme-icon="dark" class={theme === "dark" ? "" : "hidden"}>
    <Moon class="h-4 w-4" />
  </span>

</button> -->

<button
  type="button"
  aria-label={label}
  data-theme-toggle
  data-theme-state={theme}
  class={`relative inline-flex h-11 w-11 items-center justify-center rounded-2xl border border-white/10 bg-white/[0.03] text-white/80 backdrop-blur-xl transition-all duration-300 hover:border-white/20 hover:bg-white/[0.05] ${Astro.props.class || ""}`}
>
  <Monitor class="theme-icon theme-system h-4 w-4" />
  <Sun class="theme-icon theme-light h-4 w-4" />
  <Moon class="theme-icon theme-dark h-4 w-4" />
</button>